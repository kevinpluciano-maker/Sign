// [DEPRECATED] Old drag-and-drop page editor — now a simple passthrough.
import { ReactNode } from 'react';

const PageEditor = ({ children }: { children: ReactNode }) => <>{children}</>;
export default PageEditor;
