// [DEPRECATED] Old floating admin toolbar — removed in favor of /admin route.
// Kept as a no-op to avoid breaking imports in legacy page files.
const EditorToolbar = () => null;
export default EditorToolbar;
